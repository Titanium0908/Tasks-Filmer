import { StyleSheet } from 'react-native';

const pageStyles = StyleSheet.create({
    container:{
      flex: 7,
    },
    scrollView:{
      // maxHeight: 100,
      flex: 1,
      backgroundColor: "#aaa",
    }
});

export default pageStyles;